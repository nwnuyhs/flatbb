<?php
/**
 * English is the source language: every t('...') key is already English, so this file is empty.
 * To add a language run `php flatbb lang:sync <code>` (e.g. de, fr, pt-br): it writes lang/<code>.php
 * with every string, then fill in the values (empty = English is shown). Set '__name' to the native
 * name; the pack then appears in Admin → Settings → General → Language.
 */
return [
    '__name' => 'English',
];
