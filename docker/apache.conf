# FlatBB's .htaccess carries the clean URLs and blocks the private folders: let it work.
<Directory /var/www/html>
    AllowOverride All
    Options -Indexes
</Directory>
ServerTokens Prod
ServerSignature Off
